# Lambda Runtime Dependencies
# These are packaged into Lambda layers or deployment packages
pyyaml>=6.0.1
langchain>=0.1.0
langchain-aws>=0.1.0
langchain-community>=0.1.0
opensearch-py>=2.4.0
PyGithub>=2.1.0
requests>=2.31.0
urllib3>=2.0.0

# AWS SDK (boto3 is provided by Lambda runtime, but we specify for local dev)
boto3>=1.34.0
botocore>=1.34.0

# Testing Dependencies (not included in Lambda deployment)
pytest>=7.4.0
hypothesis>=6.92.0
pytest-cov>=4.1.0
moto>=4.2.0
