# Lambda Layer Dependencies
# These dependencies are packaged into a Lambda layer for reuse across functions
# boto3 and botocore are excluded as they're provided by the Lambda runtime

pyyaml>=6.0.1
langchain>=0.1.0
langchain-aws>=0.1.0
langchain-community>=0.1.0
opensearch-py>=2.4.0
PyGithub>=2.1.0
requests>=2.31.0
urllib3>=2.0.0
aws-xray-sdk>=2.12.0
